name = "lwt_pipe"
version = "0.1"
requires = "result lwt"
description = "An alternative to `Lwt_stream` with interfaces for producers and
consumers and a bounded internal buffer."

archive(byte) = "lwt_pipe.cma"
archive(byte, plugin) = "lwt_pipe.cma"
archive(native) = "lwt_pipe.cmxa"
archive(native, plugin) = "lwt_pipe.cmxs"
exists_if = "lwt_pipe.cma"

